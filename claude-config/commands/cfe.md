---
description: Concept Family Explorer — map a subject's full conceptual family, score gaps via the da-* skills, loop /dr on every viable gap until the concept tree saturates, then optimize
argument-hint: subject [--budget-minutes=N|--max-iter=N|--json]
---

Read ~/.claude/skills/concept-family-explorer/SKILL.md and execute it against $ARGUMENTS, flags included. The SKILL.md is the single source of truth; do not re-specify its steps here.

If $ARGUMENTS is empty, ask once for the subject to explore, then continue.
