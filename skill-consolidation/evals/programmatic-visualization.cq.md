# Competency questions — programmatic visualization family

1. How do I render a Mermaid diagram to PNG/SVG from the command line or CI? -> A
2. Which text-to-diagram DSL (Mermaid/Graphviz/PlantUML/D2) fits a given diagram, and how do I render it headlessly? -> A
3. How do I generate a mindmap programmatically from an outline/JSON (Markmap, OPML, jsMind)? -> B
4. Mind map vs concept map — which structure and which tool/format? -> B
5. How do I build a wordcloud from a text corpus (d3-cloud / Python wordcloud), and when should I NOT use one? -> C
6. Which graph layout algorithm and library for a network diagram (force-directed vs hierarchical; Cytoscape/sigma/D3-force)? -> D
7. How do I render a 3D / WebGL knowledge graph or "3D mindmap" (three.js, 3d-force-graph, deck.gl)? -> D
8. How do I implement a chart in code with D3 / Vega-Lite / Plotly / Observable Plot, and render it server-side? -> E
9. SVG vs Canvas vs WebGL — which rendering substrate for N data points? -> D/E
10. How can an LLM generate a valid diagram spec (text->mermaid) with a validation/repair loop? -> A (folded F)
