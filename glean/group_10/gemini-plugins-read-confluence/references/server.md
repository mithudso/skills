# MongoDB Server — Confluence Reading Context

## Common Spaces
- `SERVER` — MongoDB Server engineering docs
- `DRIVERS` — MongoDB drivers

## Frequently Referenced Pages
- Build system guide: search `bazel build` in SERVER space
- Resmoke guide: search `resmoke` in SERVER space
- Evergreen guide: search `evergreen patch` in SERVER space

## CQL Examples
```
space = "SERVER" AND title ~ "resmoke"
space = "SERVER" AND label = "build-system"
```
