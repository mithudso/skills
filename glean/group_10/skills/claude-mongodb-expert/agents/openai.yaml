interface:
  display_name: "MongoDB Expert"
  short_description: "MongoDB Expert reference generated from docs/mongodb-expert-context.md; use when the request needs this dom..."
  brand_color: "#2563EB"
  default_prompt: "Use the bundled MongoDB Expert context and its cited sources to answer the user's request."
policy:
  allow_implicit_invocation: true
