---
description: Document Deep Optimizer — multi-pass critique that applies every Medium+ fix in place and loops to convergence
argument-hint: file path [--voice-only|--minimal|--explain|--annotate|--report|--read-only|--cross-model|--budget-minutes=N]
---

Read ~/.claude/skills/writing-expert/references/ddo/SKILL.md and execute it against $ARGUMENTS, flags included. The SKILL.md is the single source of truth; do not re-specify its steps here.

If $ARGUMENTS is empty, ask once for the file path, then continue.
