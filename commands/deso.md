---
description: Design Deep Optimizer — multi-pass design/UI critique-and-fix over a screenshot, URL, code, or spec; applies Medium+ fixes to convergence for code-backed designs
argument-hint: screenshot, URL, code path, or spec [--read-only|--structural-only|--report|--annotate|--dry-run|--no-sync|--max-iter=N|--budget-minutes=N]
---

Read ~/.claude/skills/design-deep-optimizer/SKILL.md and execute it against $ARGUMENTS, flags included. The SKILL.md is the single source of truth; do not re-specify its steps here.

If $ARGUMENTS is empty, ask once for the design input (screenshot, URL, code path, or spec), then continue.
