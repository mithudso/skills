---
description: Deep MongoDB MQL Query Optimizer — multi-pass find/aggregation review-and-fix that applies every Medium+ rewrite in place and verifies via explain
argument-hint: MQL filter, pipeline, or file path [--read-only|--minimal|--explain|--annotate|--report|--apply-index|--no-verify]
---

Read ~/.claude/skills/deep-mongodb-mql-query-optimizer/SKILL.md and execute it against $ARGUMENTS, flags included. The SKILL.md is the single source of truth; do not re-specify its steps here.

If $ARGUMENTS is empty, ask once for the MQL filter, pipeline, or file path, then continue.
