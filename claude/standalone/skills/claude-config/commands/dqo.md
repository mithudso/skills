---
description: Deep Query Optimizer — multi-pass SQL query review-and-fix that applies every Medium+ rewrite in place and verifies via EXPLAIN
argument-hint: query or file path [--read-only|--minimal|--explain|--annotate|--report|--no-verify]
---

Read ~/.claude/skills/deep-query-optimizer/SKILL.md and execute it against $ARGUMENTS, flags included. The SKILL.md is the single source of truth; do not re-specify its steps here.

If $ARGUMENTS is empty, ask once for the query or file path, then continue.
